public class Solution {
    public int MissingMultiple(int[] nums, int k) {
        int n = nums.Length;
        var s = new System.Collections.Generic.HashSet<int>(nums);
        int m = k;
        while (true) {
            if (!s.Contains(m)) return m;
            m += k;
        }
    }
}
