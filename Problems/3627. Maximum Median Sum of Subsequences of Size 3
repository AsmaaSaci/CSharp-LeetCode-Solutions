public class Solution {
    public long MaximumMedianSum(int[] nums) {
        Array.Sort(nums);
        var rs = 0L;
        for (int i = 0; i < nums.Length / 3; i++)
        {
            rs += nums[nums.Length - 2 - 2 * i];
        }
        return rs;
    }
}
