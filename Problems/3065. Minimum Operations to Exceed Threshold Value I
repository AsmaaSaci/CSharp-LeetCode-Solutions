public class Solution {
    public int MinOperations(int[] nums, int k) {
        int count = 0;
        foreach (int i in nums) {
            if (i < k) {
                count++;
            }
        }
        return count;
    }
}
