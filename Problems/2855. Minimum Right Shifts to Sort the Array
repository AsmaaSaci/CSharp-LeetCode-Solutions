public class Solution
{
    public int MinimumRightShifts(IList<int> nums)
    {
        var shifts = 0;

        for (var i = 1; i <= nums.Count; i++)
            if (nums[i - 1] > nums[i % nums.Count])
                shifts = shifts == 0 ? nums.Count - i : -1;

        return shifts;
    }
}
