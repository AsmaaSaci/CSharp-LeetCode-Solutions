public class Solution {
    public int AbsDifference(int[] nums, int k) {
        Array.Sort(nums);
        var res = 0;
        var last = nums.Length-1;
        for(int i=0;i<k;i++)
            res += nums[last-i]-nums[i];
        
        return res;
    }
}
