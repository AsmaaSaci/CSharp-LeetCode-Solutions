public class Solution {
    public int FindLucky(int[] arr) {
        var map = new int[501];
        foreach(var num in arr)
            map[num]++;
        for(int i=map.Length -1 ;i>0;i--)
            if(i == map[i])
                return i;
        return -1;
    }
}
